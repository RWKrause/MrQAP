% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpu_utils.R
\name{gpu_solve_fixed_x}
\alias{gpu_solve_fixed_x}
\title{Batched OLS with a fixed design matrix}
\usage{
gpu_solve_fixed_x(X, Y, device = "cpu")
}
\arguments{
\item{X}{Numeric matrix (n x p), including the intercept column.}

\item{Y}{Numeric matrix (n x B) of responses, one per permutation.}

\item{device}{Character; \code{"cuda"} or \code{"cpu"}.}
}
\value{
A list with \code{coefficients} and \code{t}, each \code{p x B}.
}
\description{
Solves \code{ncol(Y)} regressions of the columns of \code{Y} on a single
\code{X}.  Because the design does not change, its projection is formed
once and the whole batch is one matrix product.
}
\details{
Kept separate from \code{gpu_batch_ols()} so the torch algebra can be
checked directly against \code{qap_ols_solve()} -- see
\code{tests/testthat/test-gpu.R}.
}
\keyword{internal}
