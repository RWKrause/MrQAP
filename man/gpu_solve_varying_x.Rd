% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpu_utils.R
\name{gpu_solve_varying_x}
\alias{gpu_solve_varying_x}
\title{Batched OLS with a design matrix that changes per permutation}
\usage{
gpu_solve_varying_x(X, col, Pcols, y, device = "cpu")
}
\arguments{
\item{X}{Numeric matrix (n x p); the design, whose column \code{col} is
replaced per permutation.}

\item{col}{Integer; index of the column that varies.}

\item{Pcols}{Numeric matrix (n x B); the permuted values of that column,
one column per permutation.}

\item{y}{Numeric response vector of length n.}

\item{device}{Character; \code{"cuda"} or \code{"cpu"}.}
}
\value{
A list with \code{coefficients} and \code{t}, each \code{p x B}.
}
\description{
Solves \code{dim(Xb)[1]} regressions of a single \code{y} on the stacked
designs in \code{Xb}, using batched tensor operations rather than one
small solve per permutation.
}
\details{
Kept separate from \code{gpu_batch_ols()} so the torch algebra can be
checked directly against \code{qap_ols_solve()}.
}
\keyword{internal}
