% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/QAPcss.R
\name{QAPcss}
\alias{QAPcss}
\title{Parameter and p-value estimation with MRQAP for CSS data}
\usage{
QAPcss(
  formula,
  data,
  mode = "directed",
  diag = FALSE,
  nullhyp = "qapy",
  reps = 1000,
  seed = NULL,
  ncores = NULL,
  family = "gaussian",
  estimator = "standard",
  groups = NULL,
  fixest_se_cluster = NULL,
  reference = NULL,
  comparison = NULL,
  use_robust_errors = FALSE,
  random_intercept_nets = FALSE,
  random_intercept_sender = FALSE,
  random_intercept_receiver = FALSE,
  random_intercept_perceiver = FALSE,
  use_gpu = FALSE
)
}
\arguments{
\item{formula}{A formula describing the model.
The left-hand side is the dependent variable name in \code{data};
the right-hand side lists predictor variable names.
Use \code{|} (without parentheses) for fixest fixed effects.
Use \code{(1|var)} for lme4-style random effects.}

\item{data}{Named list; each element is a cubic \code{array} (or a
\code{list} of arrays if there are multiple CSS').  Names must
correspond to the variables in \code{formula}.}

\item{mode}{Character; \code{"directed"} (default) or \code{"undirected"}.}

\item{diag}{Logical; include diagonal values (default \code{FALSE}).}

\item{nullhyp}{Character; \code{"qapy"} (default) or \code{"qapspp"}.}

\item{reps}{Integer; number of permutations (default 1000).}

\item{seed}{Integer; optional random seed.}

\item{ncores}{Integer; cores for parallel processing via the future
framework.}

\item{family}{Character; \code{"gaussian"} (default), \code{"binomial"},
\code{"poisson"}, \code{"negbin"} (negative binomial),
\code{"zip"} (zero-inflated Poisson), or \code{"multinom"}.}

\item{estimator}{Character; \code{"standard"} (default) or \code{"gmm"}
(for binomial, poisson, negbin, and zip families).}

\item{groups}{Vector or list; permutation grouping.}

\item{fixest_se_cluster}{Character; cluster variable for fixest.}

\item{reference}{Character or numeric; reference group for multinomial.}

\item{comparison}{Named list of length-2 character vectors for accuracy
comparisons (e.g., \code{list(commission = c("false_positive",
"true_negative"))}).}

\item{use_robust_errors}{Logical; use HC3 correction.}

\item{random_intercept_nets, random_intercept_sender, random_intercept_receiver, random_intercept_perceiver}{Logical; add random intercepts.}

\item{use_gpu}{Logical; use GPU-accelerated batch OLS (gaussian only,
requires \code{torch} package).}
}
\value{
An object of class \code{QAPCSS}.
}
\description{
Estimates regression models on cubic Cognitive Social Structure (CSS) data
(3D arrays of format \code{[sender, receiver, perceiver]}) with
permutation-based inference.
}
