% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qap_ols.R
\name{qap_valid_mask}
\alias{qap_valid_mask}
\title{Which cells of a network enter the model}
\usage{
qap_valid_mask(
  d,
  css = FALSE,
  mode = "directed",
  diag = FALSE,
  multi_mode = FALSE
)
}
\arguments{
\item{d}{Integer vector of dimensions.}

\item{css}{Logical; a 3-D CSS array?}

\item{mode}{Character; \code{"directed"} or \code{"undirected"}.}

\item{diag}{Logical; keep the diagonal?}

\item{multi_mode}{Logical; is each dimension its own node set?}
}
\value{
A logical vector, one element per cell, in the package's
  vectorisation order.
}
\description{
The structural exclusions, in one place: the diagonal (self-ties), and
the lower triangle in undirected mode, where each dyad must contribute
exactly one observation. Neither applies to multi-mode data, where the
dimensions index disjoint node sets and there is no diagonal to drop and
no reciprocal dyad to deduplicate.
}
\details{
Shared by \code{qap_ols_template()}, which needs the mask to vectorise
the data, and by \code{predict.QAP()}, which needs it to put a vector of
fitted values back into the shape of the network.
}
\keyword{internal}
