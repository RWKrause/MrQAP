% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{as.data.frame.QAP}
\alias{as.data.frame.QAP}
\title{Coerce a QAP fit to a data frame}
\usage{
\method{as.data.frame}{QAP}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{A QAP fit.}

\item{row.names, optional}{Ignored; present for method consistency.}

\item{...}{Ignored.}
}
\value{
A data frame.  For a comparison model the tables are stacked with
  a \code{comparison} column.
}
\description{
The tidy form of the coefficient table: one row per term, with the
estimate, test statistic and permutation p-values.  Convenient for
assembling results across models.
}
\examples{
set.seed(1)
n  <- 10
x1 <- matrix(rnorm(n^2), n, n)
x2 <- matrix(rnorm(n^2), n, n)
d  <- list(y = 0.5 * x1 + matrix(rnorm(n^2), n, n), x1 = x1, x2 = x2)
as.data.frame(QAP(y ~ x1 + x2, data = d, reps = 50))
}
